<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>Seite reservieren</title>

    <style>
        body {
            margin: 0;
            font-family: "Segoe UI", Arial, sans-serif;
            background: radial-gradient(circle at top, #1a1a1a, #000);
            color: #d4af37;
        }

        main {
            min-height: 85vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        /* LOGO */
        .logo {
            margin-bottom: 30px;
        }

        .logo img {
            max-width: 260px;
            height: auto;
        }

        .lux-box {
            background: linear-gradient(145deg, #0e0e0e, #1a1a1a);
            border: 2px solid #d4af37;
            border-radius: 16px;
            padding: 40px;
            width: 360px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.8);
            text-align: center;
        }

        .lux-box h2 {
            margin-bottom: 30px;
            font-weight: 600;
            letter-spacing: 1px;
        }

        .lux-box input {
            width: 100%;
            padding: 14px;
            margin-bottom: 18px;
            background: transparent;
            border: 1px solid #d4af37;
            border-radius: 8px;
            color: #fff;
            font-size: 15px;
        }

        .lux-box input::placeholder {
            color: #bfa84a;
        }

        .lux-box button {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #d4af37, #b8962e);
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            color: #000;
            letter-spacing: 1px;
        }

        .lux-box button:hover {
            background: linear-gradient(135deg, #e6c65c, #d4af37);
        }

        footer {
            border-top: 1px solid #d4af37;
            padding: 18px;
            display: flex;
            justify-content: center;
            gap: 40px;
            font-size: 15px;
        }

        footer a {
            color: #d4af37;
            text-decoration: none;
            font-weight: 500;
        }

        footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<main>

    <!-- LOGO -->
    <div class="logo">
        <img src="logo-yazan.png" alt="Yazan Logo">
    </div>

    <!-- RESERVIERUNG -->
    <div class="lux-box">
        <h2>Seite reservieren</h2>

        <input type="text" placeholder="Name">
        <input type="email" placeholder="E-Mail">

        <button>JETZT RESERVIEREN</button>
    </div>

</main>

<footer>
    <div>
        📞 <a href="https://wa.me/4915737119471">015737119471</a>
    </div>
    <div>
        📸 <a href="https://instagram.com/yazan_nrw" target="_blank">yazan_nrw</a>
    </div>
</footer>

</body>
</html>
